language: node_js
node_js: lts/*
sudo: false
branches:
  only:
    - master
addons:
  chrome: stable
script:
  - yarn lint
  - yarn test
  - yarn build
install:
  - yarn install
env:
  matrix:
    secure: $GITHUB_TOKEN
notifications:
  email:
    - mandy.dwivedi@gmail.com
cache:
  yarn: true
  directories:
    - node_modules
